/**
 * 站点根路径配置。仅需配置线上部署的子路径（如 GitHub Pages 项目页）。
 * 本地通过 localhost / 127.0.0.1 访问时会自动使用根路径，无需修改此文件。
 * - 子路径部署（如 https://xxx.github.io/DarkSourceWeb/）：设为 "/DarkSourceWeb/"
 * - 根路径部署：设为 ""
 */
window.SITE_BASE = "/DarkSourceWeb/";
